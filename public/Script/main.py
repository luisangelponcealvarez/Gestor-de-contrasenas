# Importar la clase principal App del archivo app.py
from app import App

if __name__ == "__main__":
    # Crear una instancia de la clase App para iniciar la aplicación
    app = App()
    app.mainloop()
